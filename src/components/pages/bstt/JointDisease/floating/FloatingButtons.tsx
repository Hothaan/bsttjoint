import FloatingButtonsContainer from "@/components/ui/float/FloatingButtonsContainer";

export default function FloatingButtons() {
  return <FloatingButtonsContainer branch={`bstt`} page={`sub`} />;
}
